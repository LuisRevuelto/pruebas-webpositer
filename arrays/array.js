const lista = [1,2,3,4]

const lista2 = [1, true, 'ejemplo']

const arr2d = [
  [1,2,3],
  [4,5,6]
]

lista[3] // 4

lista[lista.length - 1]

lista.slice(0,2)

// loop
for(let index = 0; index < lista.length; index++) {
  console.log(lista[index])
}